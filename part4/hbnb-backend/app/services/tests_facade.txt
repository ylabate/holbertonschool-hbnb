>>> from app import create_app, db
>>> from app.services.facade import HBnBFacade
>>> app = create_app()
>>> ctx = app.app_context()
>>> ctx.push()
>>> db.drop_all()
>>> db.create_all()
>>> facade = HBnBFacade()

# Users
>>> user1 = facade.create_user({"first_name": "John", "last_name": "Doe", "email": "john@example.com", "password": "password123"})
>>> user2 = facade.create_user({"first_name": "Jane", "last_name": "Smith", "email": "jane@example.com", "password": "password123"})
>>> facade.get_user(user1.id).email
'john@example.com'
>>> facade.get_user_by_email("jane@example.com").first_name
'Jane'

# Amenities
>>> amenity1 = facade.create_amenity({"name": "Wi-Fi", "description": "High speed"})
>>> amenity2 = facade.create_amenity({"name": "Pool"})
>>> amenity2.description
''
>>> len(facade.get_all_amenities())
2

# Places
>>> place1 = facade.create_place({"title": "Cozy Apartment", "description": "Nice place", "price": 100.0, "latitude": 40.7128, "longitude": -74.0060, "user_id": user1.id, "amenities": [amenity1.id]})
>>> facade.get_place(place1.id).title
'Cozy Apartment'
>>> len(place1.amenities)
1

>>> try:
...     facade.create_place({"title": "Bad Place", "description": "Test", "price": 100.0, "latitude": 40.0, "longitude": -74.0, "user_id": "00000000-0000-0000-0000-000000000000", "amenities": []})
... except ValueError as e:
...     print(e)
owner id is not a valid id

# Reviews
>>> review1 = facade.create_review({"text": "Great place!", "rating": 5, "user_id": user2.id, "place_id": place1.id})
>>> review1.user.id == user2.id
True
>>> facade.get_review(review1.id).text
'Great place!'
>>> len(facade.get_reviews_by_place(place1.id))
1

>>> updated_review = facade.update_review(review1.id, {"text": "Updated review!", "rating": 4})
>>> updated_review.rating
4

>>> facade.delete_review(review1.id)
>>> facade.get_review(review1.id) is None
True

>>> ctx.pop()
